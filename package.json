{
  "name": "root",
  "private": true,
  "scripts": {
    "publish": "lerna publish from-package --skip-git"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
