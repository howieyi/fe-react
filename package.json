{
  "name": "root",
  "private": true,
  "scripts": {
    "publish": "lerna publish from-package --skip-git",
    "build:storage": "lerna workspace storage build:esm"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "tslib": "^2.3.0",
    "typescript": "^4.3.5"
  }
}
