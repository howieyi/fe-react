{
  "name": "root",
  "private": true,
  "scripts": {
    "publish": "lerna publish from-package --skip-git",
    "link:iwr": "yarn workspace iwr link",
    "build:storage": "yarn workspace @iosecret/storage build:esm",
    "build:wechat": "yarn workspace @iosecret/wechat build:esm"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "tslib": "^2.3.0",
    "typescript": "^4.3.5"
  }
}
