# `@iosecret/template`

> 常用项目模板统一管理

## Usage

```javascript
const { getTemplateList, generateTemplate, generateRule } = require('@iosecret/template');

// 获取模板列表
getTemplateList();

// 生成工程模板
generateTemplate
```
